import { TransformarDataDirective } from './transformar-data.directive';

describe('TransformarDataDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformarDataDirective();
    expect(directive).toBeTruthy();
  });
});
